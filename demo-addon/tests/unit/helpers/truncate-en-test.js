import { truncateEn } from '../../../helpers/truncate-en';
import { module, test } from 'qunit';

module('Unit | Helper | truncate en');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = truncateEn(42);
  assert.ok(result);
});
